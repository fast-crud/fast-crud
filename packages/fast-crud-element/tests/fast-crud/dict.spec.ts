import { expect } from 'chai'
import { getDictData } from '@fast-crud/fast-crud'

describe('dict', () => {
  it('getData', () => {
    getDictData({ url: '/test' })
    // expect(wrapper.text()).to.include(msg)
  })
})
