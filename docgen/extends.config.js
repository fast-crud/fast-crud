module.exports = {
    componentsRoot: 'packages/fast-crud-extends/src/lib', // the folder where CLI will start searching for components.
    components: '**/*.vue', // the glob to define what files should be documented as components (relative to componentRoot)
    outDir: 'docs/api/components/extends', // folder to save components docs in (relative to the current working directry)
}
